<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
    <link rel="stylesheet" href="fonts/fontawesome/css/all.css">
  </head>
  <body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <a href="/" class="navbar-brand">Bookstore</a>
            <a href="/cart" class="navbar-text text-white">
                <span class="fw-bold">Корзина</span>
                <span class="px-2">/</span>
                <span class="fw-bold">${{totalPrice}}</span>
                <i class="fas fa-shopping-basket px-2"></i>
            </a>
        </div>
    </nav>

    <div class="container py-10">
      <h1 class="text-indigo-500 display-4 mb-6 fw-bold">Итог</h1>
      {{#if cart.length}}
      <table class="table mb-6">
        <thead>
          <tr class="bg-gray-200 text-gray-600 text-uppercase fw-bold text-sm">
            <th class="py-3 px-6 text-start">Название</th>
            <th class="py-3 px-6 text-start">Автор</th>
            <th class="py-3 px-6 text-start">Цена</th>
          </tr>
        </thead>
        <tbody class="text-gray-600 text-sm">
          {{#each cart}}
          <tr class="border-bottom border-gray-200">
            <td class="py-2 px-4">{{this.title}}</td>
            <td class="py-2 px-4">{{this.author}}</td>
            <td class="py-2 px-4">${{this.price}}</td>
          </tr>
          {{/each}}
        </tbody>
        <tfoot class="bg-gray-200 text-gray-600 text-uppercase fw-bold text-sm">
          <tr>
            <td class="py-3 px-6 text-start">Общее:</td>
            <td></td>
            <td class="py-3 px-6 text-start">${{totalPrice}}</td>
          </tr>
        </tfoot>
      </table>
      <form action="/checkout-done" method="post">
        <div class="mb-4">
          <label for="name" class="form-label text-gray-500 fw-semibold">Имя:</label>
          <input type="text" id="name" name="name" class="form-control shadow-none" required>
        </div>
        <div class="mb-4">
          <label for="address" class="form-label text-gray-500 fw-semibold">Адресс:</label>
          <input type="text" id="address" name="address" class="form-control shadow-none" required>
        </div>
        <div class="mb-4">
          <label for="phone" class="form-label text-gray-500 fw-semibold">Телефон:</label>
          <input type="text" id="phone" name="phone" class="form-control shadow-none" required>
        </div>
        <button type="submit" class="btn btn-primary fw-bold">
          Оформить заказ
        </button>
      </form>
      {{else}}
      <p class="text-gray-500 fw-semibold lead">Ваша корзина пуста.</p>
      {{/if}}
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN" crossorigin="anonymous"></script>
  </body>
</html>